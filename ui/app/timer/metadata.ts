import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "TitleCard Timer | MediaVerse",
  description: "Use the TitleCard Timer to capture the exact moment the title appears in each movie.",
  openGraph: {
    title: "TitleCard Timer | MediaVerse",
    description: "Use the TitleCard Timer to capture the exact moment the title appears in each movie.",
    images: ["/opengraph-image"],
  },
  twitter: {
    card: "summary_large_image",
    title: "TitleCard Timer | MediaVerse",
    description: "Use the TitleCard Timer to capture the exact moment the title appears in each movie.",
    images: ["/opengraph-image"],
  },
}
