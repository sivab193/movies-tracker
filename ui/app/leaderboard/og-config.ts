import type { PageOg } from "@/lib/og/static"

export const OG_LEADERBOARD: PageOg = {
  "kind": "Leaderboard",
  "title": "Global Leaderboard",
  "emoji": "🏆",
  "subtitle": "Real-time rankings from exact runtime seconds",
  "facts": [
    "Annual season",
    "Live rankings"
  ],
  "description": "See who has spent the most time watching movies this season. Rankings are calculated from exact runtime seconds."
}
