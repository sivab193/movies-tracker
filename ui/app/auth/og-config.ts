import type { PageOg } from "@/lib/og/static"

export const OG_AUTH: PageOg = {
  "kind": "Account",
  "title": "Sign in",
  "emoji": "🔐",
  "subtitle": "Track every film you watch",
  "description": "Sign in to MediaVerse to log your watch history, track series and compete on the global leaderboard."
}
