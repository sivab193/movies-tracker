import type { PageOg } from "@/lib/og/static"

export const OG_DEVICE: PageOg = {
  "kind": "Account",
  "title": "Device Authorization",
  "emoji": "🤖",
  "private": true,
  "subtitle": "Connect Claude and other AI assistants",
  "description": "Authorize a CLI or AI assistant to access your MediaVerse account via the MCP server."
}
