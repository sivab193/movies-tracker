import type { PageOg } from "@/lib/og/static"

export const OG_SETTINGS: PageOg = {
  "kind": "Account",
  "title": "Settings",
  "emoji": "⚙️",
  "private": true,
  "subtitle": "Username, preferences and account",
  "description": "Manage your MediaVerse username, preferences and account settings."
}
